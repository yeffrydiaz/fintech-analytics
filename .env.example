DATABASE_URL="postgresql://user:password@localhost:5432/fintech_analytics"
ENCRYPTION_KEY="your-32-byte-encryption-key-here!!"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key"
# Demo credentials — only active when ENABLE_DEMO_AUTH=true AND NODE_ENV != production.
# Remove or leave unset in any publicly accessible deployment.
ENABLE_DEMO_AUTH=true
DEMO_PASSWORD="change-me-before-use"
